# 10: API documentation from the shared schema

**What to build:** Browsable API documentation a reviewer can read without opening the source, generated from the shared rules so it cannot drift away from them.

**Blocked by:** 08

**Status:** ready-for-agent

- [ ] Browsable API documentation is served
- [ ] It is generated from the shared creation rules rather than written separately
- [ ] It reflects the Conditional Requirement without that rule being restated by hand
- [ ] Every endpoint the directory uses appears in it
